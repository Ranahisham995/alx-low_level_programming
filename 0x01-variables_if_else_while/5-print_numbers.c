#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print all the alphabet letters
 *
 * Return: always 0 (success)
*/

int main(void)
{
	int n = 0;

	while (n <= 9)
	{
		printf("%i", n);
		n++;
	}
	printf("\n");

	return (0);
}
